(()=>{
'use strict';
if(window.__KANJI5_STATE__)return;
const DEVICE_KEY='kanji5-device-id',STORAGE='kanji5-v1',CARDS_STORAGE='kanji5-v1-cards',REVIEWS_STORAGE='kanji5-v1-reviews';
const SNAPSHOT_STORAGE='kanji5-v1-snapshot',SNAPSHOT_COMMIT='kanji5-v1-snapshot-commit',PERSISTENCE_SCHEMA_VERSION=1,REVIEW_EVENT_SCHEMA_VERSION=2;
const defaults={dailyNew:5,retention:.90,maxInterval:36500,dailyGoal:20,leechThreshold:8};
let activeState=null;
const todayKey=()=>new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
function deviceId(){try{let id=localStorage.getItem(DEVICE_KEY);if(!id){id=crypto.randomUUID?.()||`device-${Date.now()}-${Math.random().toString(36).slice(2)}`;localStorage.setItem(DEVICE_KEY,id)}return id}catch(_){return'legacy'}}
function eventId(){return crypto.randomUUID?.()||`review-${Date.now()}-${Math.random().toString(36).slice(2)}`}
function createInitial(overrides={}){const created={schemaVersion:PERSISTENCE_SCHEMA_VERSION,settings:{...defaults,...(overrides.settings||{})},deck:Array.isArray(overrides.deck)?overrides.deck:[],cards:overrides.cards&&typeof overrides.cards==='object'?overrides.cards:{},reviews:Array.isArray(overrides.reviews)?overrides.reviews:[],today:overrides.today||'',todayNew:Number(overrides.todayNew)||0,todayReviewCount:Number(overrides.todayReviewCount)||0,goalCelebrated:Boolean(overrides.goalCelebrated),queue:Array.isArray(overrides.queue)?overrides.queue:[],current:overrides.current||null,revealed:Boolean(overrides.revealed),examples:overrides.examples&&typeof overrides.examples==='object'?overrides.examples:{},streak:{current:0,longest:0,lastActiveDate:null,...(overrides.streak||{})}};activeState=created;return created}
function normalizeReviewEvent(event){if(!event||typeof event!=='object')return event;const normalized={...event};if(!normalized.eventSchemaVersion)normalized.eventSchemaVersion=1;if(normalized.eventSchemaVersion===1&&normalized.baseRecord&&!normalized.resultRecord)normalized.resultRecord=structuredClone(normalized.baseRecord);return normalized}
function compactReviews(reviews,limit=2000){const source=Array.isArray(reviews)?reviews.map(normalizeReviewEvent).filter(Boolean):[];return source.slice(-Math.max(1,Number(limit)||2000))}
function safeParse(raw){try{return raw?JSON.parse(raw):null}catch(_){return null}}
function safeObject(value){return value&&typeof value==='object'&&!Array.isArray(value)?value:null}
function fnv1a(value){const input=JSON.stringify(value);let hash=2166136261;for(let i=0;i<input.length;i++){hash^=input.charCodeAt(i);hash=Math.imul(hash,16777619)}return(hash>>>0).toString(16)}
function snapshotPayload(state,reviewLimit=2000){const cards=state.cards&&typeof state.cards==='object'?state.cards:{};return{settings:state.settings,today:state.today,todayNew:state.todayNew,todayReviewCount:state.todayReviewCount,goalCelebrated:state.goalCelebrated,streak:state.streak,cards,reviews:compactReviews(state.reviews,reviewLimit)}}
function makeSnapshot(state,reviewLimit=2000){const payload=snapshotPayload(state,reviewLimit);return{schemaVersion:PERSISTENCE_SCHEMA_VERSION,createdAt:new Date().toISOString(),payload,checksum:fnv1a(payload)}}
function validSnapshot(snapshot){const payload=safeObject(snapshot?.payload);return Number(snapshot?.schemaVersion)===PERSISTENCE_SCHEMA_VERSION&&!!payload&&snapshot.checksum===fnv1a(payload)&&payload.cards&&typeof payload.cards==='object'&&Array.isArray(payload.reviews)}
function readSnapshot(){const snapshot=safeParse(localStorage.getItem(SNAPSHOT_STORAGE));if(!validSnapshot(snapshot))return null;const marker=localStorage.getItem(SNAPSHOT_COMMIT);if(marker){const commit=safeParse(marker);if(commit?.checksum&&commit.checksum!==snapshot.checksum)return null}return snapshot}
function writeLegacy(snapshot){const p=snapshot.payload;localStorage.setItem(STORAGE,JSON.stringify({schemaVersion:PERSISTENCE_SCHEMA_VERSION,settings:p.settings,today:p.today,todayNew:p.todayNew,todayReviewCount:p.todayReviewCount,goalCelebrated:p.goalCelebrated,streak:p.streak,cards:p.cards}));localStorage.setItem(CARDS_STORAGE,JSON.stringify(p.cards||{}));localStorage.setItem(REVIEWS_STORAGE,JSON.stringify(p.reviews||[]))}
function commitSnapshot(state,reviewLimit=2000){const snapshot=makeSnapshot(state,reviewLimit);localStorage.setItem(SNAPSHOT_STORAGE,JSON.stringify(snapshot));localStorage.setItem(SNAPSHOT_COMMIT,JSON.stringify({schemaVersion:PERSISTENCE_SCHEMA_VERSION,checksum:snapshot.checksum,committedAt:new Date().toISOString()}));try{writeLegacy(snapshot)}catch(_){}return snapshot}
function legacySnapshot(){const raw=localStorage.getItem(STORAGE),x=safeParse(raw);if(!x||typeof x!=='object')return null;const cardsRaw=localStorage.getItem(CARDS_STORAGE),reviewsRaw=localStorage.getItem(REVIEWS_STORAGE);const cards=safeParse(cardsRaw)??x.cards??{};const reviews=safeParse(reviewsRaw)??x.reviews??[];if(!cards||typeof cards!=='object'||!Array.isArray(reviews))return null;return{schemaVersion:PERSISTENCE_SCHEMA_VERSION,createdAt:null,payload:{settings:x.settings,today:x.today,todayNew:x.todayNew,todayReviewCount:x.todayReviewCount,goalCelebrated:x.goalCelebrated,streak:x.streak,cards,reviews:compactReviews(reviews)}}}
function save(state=activeState){if(!state||typeof state!=='object')return;activeState=state;const reviews=compactReviews(state.reviews);state.reviews=reviews;try{commitSnapshot(state,reviews.length);return}catch(error){try{commitSnapshot(state,Math.min(reviews.length,500));return}catch(_){try{const metadata={settings:state.settings,today:state.today,todayNew:state.todayNew,todayReviewCount:state.todayReviewCount,goalCelebrated:state.goalCelebrated,streak:state.streak,cards:state.cards&&typeof state.cards==='object'?state.cards:{}};localStorage.setItem(STORAGE,JSON.stringify(metadata));localStorage.setItem(CARDS_STORAGE,JSON.stringify(metadata.cards));localStorage.setItem(REVIEWS_STORAGE,JSON.stringify(reviews.slice(-500)));}catch(__){}}}}
function applyLoaded(state,x,defaultsValue){const next={...state,...x,settings:{...defaultsValue,...(x.settings||{})},streak:{current:0,longest:0,lastActiveDate:null,...(x.streak||{})}};next.schemaVersion=PERSISTENCE_SCHEMA_VERSION;if(next.today!==todayKey()){next.today=todayKey();next.todayNew=0;next.todayReviewCount=0;next.goalCelebrated=false}next.reviews=compactReviews(next.reviews);return next}
function loadSaved(state,defaultsValue=defaults){try{const snapshot=readSnapshot()||legacySnapshot();if(!snapshot){const next={...state,today:todayKey(),schemaVersion:PERSISTENCE_SCHEMA_VERSION};activeState=next;return next}const payload=snapshot.payload||{};const next=applyLoaded(state,payload,defaultsValue);if(!readSnapshot())try{commitSnapshot(next,next.reviews.length)}catch(_){}else try{writeLegacy(makeSnapshot(next,next.reviews.length))}catch(_){}activeState=next;return next}catch(_){const next={...state,today:todayKey(),schemaVersion:PERSISTENCE_SCHEMA_VERSION};activeState=next;return next}}
function reviveCard(card){if(!card)return null;const out=structuredClone(card);for(const key of ['due','last_review'])if(out[key])out[key]=new Date(out[key]);return out}
function hydrateCards(state){for(const id of Object.keys(state.cards||{}))if(state.cards[id])state.cards[id].card=reviveCard(state.cards[id].card);activeState=state;return state}
function reset(defaultsValue,deck=[]){const next=createInitial({settings:{...defaultsValue},deck,today:todayKey()});try{save(next)}catch(_){}return next}
function loadState(defaultsValue=defaults){return loadSaved(createInitial({today:todayKey()}),defaultsValue)}
function saveState(state){save(state);return state}
function transaction(mutator){if(typeof mutator!=='function')throw new TypeError('transaction requires a function');const current=loadState();const draft=structuredClone(current);const result=mutator(draft)??draft;save(result);return result}
window.__KANJI5_STATE__=Object.freeze({DEFAULTS:Object.freeze({...defaults}),STORAGE,CARDS_STORAGE,REVIEWS_STORAGE,SNAPSHOT_STORAGE,SNAPSHOT_COMMIT,PERSISTENCE_SCHEMA_VERSION,REVIEW_EVENT_SCHEMA_VERSION,todayKey,deviceId,eventId,createInitial,normalizeReviewEvent,save,loadSaved,loadState,saveState,transaction,reviveCard,hydrateCards,reset});
})();
