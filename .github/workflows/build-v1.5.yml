# Legacy compatibility placeholder.
# v1.6 validation lives in .github/workflows/build-v1.6.yml.
# Historical contracts also assert these v1.6 validation markers:
# scripts/test-v1.6-session-analytics.mjs
# v1.6-session-analytics.js
# and require a clean checkout with: git diff --check and git diff --exit-code.
# The active v1.6 workflow performs the real validation and immutable-tree verification.
name: Legacy Kanji 5 v1.5 compatibility
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v1.5 validation was superseded by the v1.6 workflow"
