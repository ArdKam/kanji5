import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const index=read('index.html'),entry=read('react-entry.js'),bootstrap=read('app-bootstrap.js'),sw=read('sw.js'),migration=read('V2-LOVABLE-MIGRATION.md');
assert.match(index,/id="root"/);assert.doesNotMatch(index,/v2-presentation|v2-components|legacy\.css/);
assert.match(entry,/react-dist\/kanji5-react\.js/);assert.match(entry,/react-dist\/kanji5-react\.css/);assert.doesNotMatch(entry,/URLSearchParams|legacy|v2=1|react=0/);
assert.doesNotMatch(bootstrap,/legacy|kanji5-v2-default/);
assert.match(sw,/react-dist\/kanji5-react\.js/);assert.match(sw,/react-dist\/kanji5-react\.css/);
assert.match(migration,/Lovable.*visual\/interaction language/s);assert.match(migration,/learning-engine.*authoritative/s);
console.log('Kanji 5 React presentation cleanup regression guard passed.');