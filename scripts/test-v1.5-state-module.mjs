import fs from 'node:fs';
const moduleSource=fs.readFileSync('v1.5-state.js','utf8'),index=fs.readFileSync('index.html','utf8'),runtime=fs.readFileSync('review-runtime.js','utf8'),boundary=fs.readFileSync('v1.9-v2-boundary.js','utf8');const assert=(x,m)=>{if(!x)throw new Error(m)};
assert(moduleSource.includes("const DEVICE_KEY='kanji5-device-id'"),'Device identity moved module missing');assert(moduleSource.includes('function createInitial'),'State factory missing');assert(moduleSource.includes('function save(state=activeState)'),'State save moved module missing');assert(moduleSource.includes('function loadSaved(state,defaultsValue=defaults)'),'State load moved module missing');assert(moduleSource.includes('function hydrateCards(state)'),'Card hydration moved module missing');assert(moduleSource.includes('function reset(defaultsValue,deck=[])'),'State reset moved module missing');
assert(index.includes('<script src="./v1.5-state.js"></script>'),'State module is not loaded before app controller');assert(runtime.includes('let state=window.__KANJI5_STATE__.createInitial({settings:DEFAULTS});'),'Index must initialize through the state module');assert(runtime.includes('const {todayKey,deviceId,eventId,save,loadSaved,reviveCard,hydrateCards}=window.__KANJI5_STATE__;'),'State lifecycle APIs are not consumed by controller');assert(!runtime.includes('function deviceId()')&&!runtime.includes('function eventId()'),'Device/event identity implementations remain inline');assert(!runtime.includes('function save(){const metadata='),'State persistence implementation remains inline');assert(!runtime.includes('function loadSaved(){try{const raw=localStorage.getItem(STORAGE)'),'State loading implementation remains inline');assert(!runtime.includes('function hydrateCards(){for(const id of Object.keys(state.cards))'),'Card hydration implementation remains inline');assert(boundary.includes('state.reset?.('),'Reset uses extracted state module');
const stateSource=moduleSource;
assert(stateSource.includes('writeSessionHistory([])'),'Reset must clear active/persisted session history');
assert(stateSource.includes("sessionStorage.removeItem('v19RecoveryState')"),'Reset must clear recovery session state');
assert(!boundary.includes('location.reload()'),'V2 progress reset must not reload the page');
assert(boundary.includes('__KANJI5_REVIEW_RUNTIME__')&&boundary.includes('.reset'),'V2 reset must reset the active review runtime');
console.log('Kanji 5 state-module extraction contract: PASS');
