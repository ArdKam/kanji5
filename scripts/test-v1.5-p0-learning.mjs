import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const assert=(x,m)=>{if(!x)throw new Error(m)};
const index=read('index.html'),ui=read('v1.4-education-ui.js'),p0=read('v1.5-p0.js'),prod=read('v1.5-production-mcq.js'),sw=read('sw.js');
assert(index.includes('./v1.5-p0.js'),'v1.5 P0 runtime is not wired into index.html');
assert(index.includes('./v1.5-production-mcq.js'),'Dedicated production MCQ runtime is not wired into index.html');
assert(sw.includes('"./v1.5-p0.js"'),'v1.5 P0 runtime is not precached by the service worker');
assert(sw.includes('"./v1.5-production-mcq.js"'),'Production MCQ runtime is not precached by the service worker');
assert(index.includes('function jlptRank(item)'),'JLPT ranking helper is missing from the review queue');
assert(index.includes('N5:0,N4:1,N3:2,N2:3,N1:4'),'JLPT order must be N5 → N4 → N3 → N2 → N1');
assert(index.includes('jlptRank(a)-jlptRank(b)'),'New-card queue is not grouped by JLPT level');
assert(index.includes('while(start<unseen.length&&jlptRank(unseen[start])===rank)'),'New cards are not randomized within JLPT level groups');
assert(p0.includes("const COMP='kanji5-v1.5-components'"),'Component-level knowledge store missing');
assert(p0.includes('focusComponent')&&p0.includes('recordComponent'),'Component-level focus/recording logic missing');
assert(p0.includes('این بار سعی کن یک معنیِ دیگری')&&p0.includes('این بار سعی کن یک خوانشِ دیگری'),'Recall should target less-practiced components');
assert(p0.includes("b.id='v15DontKnowReview'"),'Review “don’t know” control is missing');
assert(p0.includes("ratings.querySelector('.rate.again')?.click()"),'Review “don’t know” must map to FSRS Again behavior');
assert(prod.includes("input.type='hidden'"),'Production input is not hidden');
assert(prod.includes("prompt.textContent='برای معنی زیر، کانجی مناسب را انتخاب کن.'"),'Production prompt was not changed to selection');
assert(prod.includes('data-v15-production="${esc(c.character)}"'),'Production choices are not rendered as Kanji buttons');
assert(prod.includes("input.value=b.dataset.v15Production||''"),'Production choice selection is not submitted to the backing field');
assert(prod.includes("input.dataset.v15Replaced='1'"),'Production replacement is not marked idempotently');
assert(prod.includes("input.dataset.v15Replaced==='1'||input.closest('.v14-edu-wrap')?.querySelector('.v15-production-grid')"),'Production replacement is not guarded against duplicate grids');
assert(!prod.includes('باید کانجی را بنوی'),'Production MCQ runtime still asks the user to type Kanji');
assert(!prod.includes('کانجی را بنوی'),'Production MCQ runtime still contains a typing prompt');
assert(ui.includes("edu.mode==='production'")&&ui.includes('#v14EduProductionInput'),'Original production path remains available as the controlled backing field');
console.log('Kanji 5 v1.5 P0 learning interaction checks passed.');
