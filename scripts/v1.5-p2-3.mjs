import fs from 'node:fs';
const path='index.html';let s=fs.readFileSync(path,'utf8');const assert=(x,m)=>{if(!x)throw new Error(m)};
assert(s.includes('let state={settings:{...DEFAULTS},deck:[],cards:{},reviews:[],today:"",todayNew:0,todayReviewCount:0,goalCelebrated:false,queue:[],current:null,revealed:false,examples:{},streak:{current:0,longest:0,lastActiveDate:null}};let scheduler;'),'inline state declaration marker missing');
s=s.replace('let state={settings:{...DEFAULTS},deck:[],cards:{},reviews:[],today:"",todayNew:0,todayReviewCount:0,goalCelebrated:false,queue:[],current:null,revealed:false,examples:{},streak:{current:0,longest:0,lastActiveDate:null}};let scheduler;','let state=window.__KANJI5_STATE__.createInitial({settings:DEFAULTS});let scheduler;');
const helperRe=/const \$=id=>document\.getElementById\(id\);const todayKey=\(\)=>[\s\S]*?function initScheduler\(\)\{scheduler=fsrs\(\{request_retention:state\.settings\.retention,maximum_interval:state\.settings\.maxInterval,enable_fuzz:true,enable_short_term:true,learning_steps:\["1m","10m"\],relearning_steps:\["10m"\]\}\)\}/;
const helperReplacement='const $=id=>document.getElementById(id);const {todayKey,deviceId,eventId,save,loadSaved,reviveCard,hydrateCards}=window.__KANJI5_STATE__;\nfunction initScheduler(){scheduler=fsrs({request_retention:state.settings.retention,maximum_interval:state.settings.maxInterval,enable_fuzz:true,enable_short_term:true,learning_steps:["1m","10m"],relearning_steps:["10m"]})}';
assert(helperRe.test(s),'inline state lifecycle helper block not found');s=s.replace(helperRe,helperReplacement);
const resetRe=/function resetAll\(\)\{[\s\S]*?\n\}/;assert(resetRe.test(s),'resetAll marker missing');s=s.replace(resetRe,'function resetAll(){if(!confirm("تمام پیشرفت‌ها پاک شود؟"))return;localStorage.removeItem(STORAGE);localStorage.removeItem(CARDS_STORAGE);localStorage.removeItem(REVIEWS_STORAGE);state=window.__KANJI5_STATE__.reset(DEFAULTS,state.deck);initScheduler();buildQueue();next();updateStats();toast("پیشرفت پاک شد.")}');
assert(s.includes('<script id="v1.2-dataset-bootstrap">'));const scriptMarker='<script>\nconst FSRS_URL=';assert(s.includes(scriptMarker),'main application inline script marker missing');if(!s.includes('<script src="./v1.5-state.js"></script>'))s=s.replace(scriptMarker,'<script src="./v1.5-state.js"></script>'+scriptMarker);
fs.writeFileSync(path,s);
const out=fs.readFileSync(path,'utf8');assert(out.includes('<script src="./v1.5-state.js"></script>'),'state module loader missing');assert(out.includes('let state=window.__KANJI5_STATE__.createInitial({settings:DEFAULTS});'),'inline state object was not replaced');assert(!out.includes('function loadSaved(){try{const raw=localStorage.getItem(STORAGE)'),'inline loadSaved implementation remains');assert(!out.includes('function hydrateCards(){for(const id of Object.keys(state.cards))state.cards[id].card=reviveCard(state.cards[id].card)}'),'inline hydrate implementation remains');console.log('P2-3 state lifecycle extraction patch passed.');
