v1.3 architecture work

The shipped app should load its v1.3 behavior from the application source rather than relying on Service Worker HTML rewrites. The current branch is being migrated incrementally; validation lives in build-v1.3.yml and scripts/smoke-test-v1.3.mjs.
