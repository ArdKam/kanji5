import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const index=read('index.html'),entry=read('react-entry.js'),sw=read('sw.js'),roadmap=read('V2-ROADMAP.md');
assert.match(entry,/react-dist\/kanji5-react\.js/);assert.match(entry,/react-dist\/kanji5-react\.css/);
assert.doesNotMatch(entry,/URLSearchParams|legacy|v2=1|react=0/);
assert.match(index,/id="root"/);assert.doesNotMatch(index,/v2-presentation|v2-components|legacy\.css/);
assert.match(sw,/react-dist\/kanji5-react\.js/);assert.match(sw,/react-dist\/kanji5-react\.css/);
assert.match(roadmap,/P0 — Presentation Shell/);
console.log('Kanji 5 React presentation shell contract passed.');