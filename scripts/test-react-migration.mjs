import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";

const required=["frontend/package.json","frontend/tsconfig.json","frontend/vite.config.ts","frontend/index.html","frontend/src/main.tsx","frontend/src/app/engine.ts","frontend/src/app/App.tsx","frontend/src/styles.css","docs/react-migration.md",".github/workflows/react-frontend.yml"];
for(const file of required)assert.ok(fs.existsSync(path.resolve(file)),"Missing migration file: "+file);
const app=fs.readFileSync("frontend/src/app/App.tsx","utf8");
const engine=fs.readFileSync("frontend/src/app/engine.ts","utf8");
const docs=fs.readFileSync("docs/react-migration.md","utf8");
assert.match(app,/from "\.\/engine"/);
assert.doesNotMatch(app,/__KANJI5_V19_V2_BOUNDARY__/);
assert.doesNotMatch(app,/localStorage|sessionStorage|FSRS/);
assert.match(engine,/type Boundary/);assert.match(engine,/rateLearning/);assert.match(engine,/startExercise/);assert.match(engine,/updateSettings/);
assert.match(docs,/presentation-only/);assert.match(engine,/__KANJI5_V19_V2_BOUNDARY__/);assert.match(engine,/__KANJI5_EDU_BRIDGE__/);assert.match(app,/<div className="app-shell"/);assert.match(app,/setExperience\(`dictionary`\);void action\(async\(\)=>\{await clearCustomStudyFilter\(\);await clearTransient\(\)\}\)/);
const shell=fs.readFileSync("index.html","utf8"),bootstrap=fs.readFileSync("app-bootstrap.js","utf8"),entry=fs.readFileSync("react-entry.js","utf8"),sw=fs.readFileSync("sw.js","utf8");
assert.match(shell,/id="root"/);assert.match(shell,/app-bootstrap\.js/);assert.doesNotMatch(shell,/v2-presentation|v2-components|legacy\.css|id="app"|id="loading"/);
assert.doesNotMatch(bootstrap,/legacy|kanji5-v2-default/);assert.match(bootstrap,/serviceWorker\.register/);assert.match(bootstrap,/v1\.6-session\.js/);
assert.doesNotMatch(entry,/URLSearchParams|legacy|v2=1|react=0/);assert.match(entry,/react-dist\/kanji5-react\.js/);assert.match(entry,/react-dist\/kanji5-react\.css/);
assert.match(sw,/\.\/react-dist\/kanji5-react\.js/);assert.match(sw,/\.\/react-dist\/kanji5-react\.css/);
for(const file of ["e2e/react-presentation-migration.spec.mjs","e2e/react-presentation-parity.spec.mjs","e2e/react-presentation-offline.spec.mjs"])assert.ok(fs.existsSync(file),"Missing React verification test: "+file);
console.log("Kanji 5 React migration boundary contract passed.");