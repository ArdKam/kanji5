import fs from 'node:fs';
import vm from 'node:vm';
const coreSource=fs.readFileSync('v1.4-education-core.js','utf8'),migration=fs.readFileSync('v1.4-education-migration.js','utf8'),ui=fs.readFileSync('v1.5-education-ui.js','utf8'),sync=fs.readFileSync('supabase-sync.js','utf8'),syncCore=fs.readFileSync('v1.5-education-sync-core.js','utf8'),transportCore=fs.readFileSync('v1.5-sync-core.js','utf8'),sw=fs.readFileSync('sw.js','utf8'),index=fs.readFileSync('index.html','utf8'),network=fs.readFileSync('v1.5-network.js','utf8');
const ctx={window:{},structuredClone:global.structuredClone,localStorage:{getItem(){return null},setItem(){}}};vm.createContext(ctx);vm.runInContext(coreSource,ctx);const core=ctx.window.__KANJI5_EDU_CORE__;const assert=(x,m)=>{if(!x)throw new Error(m)};
assert(core,'core missing');assert(['meaning','reading','production','vocabulary','context'].every(m=>core.registry[m]),'registry incomplete');assert(core.chooseBestExercise({character:'学'},{meaning:{attempts:9,correct:9},reading:{attempts:8,correct:2},production:{attempts:8,correct:1},vocabulary:{attempts:8,correct:1},context:{attempts:8,correct:1}},core.modes,{now:Date.now()})==='production','weakest skill was not prioritized');
const qualified=core.gradeMeaning('school work',['school work','education']);assert(qualified.correct&&qualified.quality==='exact','meaning exact grading failed');const partial=core.gradeMeaning('school',['school system']);assert(partial.correct&&partial.quality==='partial','meaning partial grading failed');assert(!core.gradeMeaning('schol',['school']).correct,'invalid typo was accepted');assert(!core.gradeMeaning('school unrelated',['school']).correct,'extra unrelated meaning text was accepted');assert(core.gradeMeaning('high school',['high school']).correct,'multi-token meaning failed');assert(core.gradeReading('gaku',['がく']).correct,'reading grading failed');assert(core.toRomaji('も')==='mo','Romaji も must map to mo');assert(core.toRomaji('もう')==='mou','Long vowel sequence もう must map to mou');assert(core.toRomaji('きょう')==='kyou','拗音 sequence きょう must map to kyou');assert(core.gradeReading('mo',['も']).correct,'Romaji grading regressed for も');
const available=core.getAvailableModes({production:true,vocabulary:true,context:true},true);assert(available.length===5,'All education modes should be available when enabled');assert(!core.getAvailableModes({production:false,vocabulary:false,context:false},false).includes('production'),'Disabled production leaked into mode list');const base={meaning:{attempts:9,correct:9},reading:{attempts:10,correct:2},production:{attempts:6,correct:1},vocabulary:{attempts:3,correct:0},context:{attempts:2,correct:1}};const ranking=core.selectExercise({character:'学'},base,available,{now:Date.now()});assert(ranking.length===5&&ranking[0].mode==='production','Adaptive engine did not prioritize the highest-value weak skill');assert(core.chooseBestExercise({character:'学'},base,available,{now:Date.now()})==='production','Best exercise selection mismatch');
const now=Date.now();const items=[{character:'学'},{character:'校'},{character:'語'}];const knowledge={'学':{meaning:{attempts:20,correct:20},reading:{attempts:20,correct:20},production:{attempts:20,correct:20},vocabulary:{attempts:20,correct:20},context:{attempts:20,correct:20},stage:'mastered'},'校':{meaning:{attempts:8,correct:2,lastAt:new Date(now-10*86400000).toISOString()}},'語':{meaning:{attempts:2,correct:2,lastAt:new Date(now-86400000).toISOString()}}};assert(typeof core.scoreEducationItem==='function'&&typeof core.selectEducationItem==='function','Adaptive Kanji selection API missing');const selected=core.selectEducationItem(items,knowledge,available,{now});assert(selected?.character==='校','Adaptive Kanji selection did not prioritize the weaker/neglected Kanji');const excluded=core.selectEducationItem(items,knowledge,available,{now,excludeCharacters:['校']});assert(excluded?.character!=='校','Adaptive Kanji selection ignored current-item exclusion');assert(core.scoreEducationItem(items[1],knowledge['校'],available,{now})>core.scoreEducationItem(items[0],knowledge['学'],available,{now}),'Weak Kanji did not outscore mastered Kanji');
const empty=core.ensureEntry({},'学',true)['学'];assert(empty.exposedAt&&empty.stage==='exposed','Exposure state initialization failed');const recorded=core.recordKnowledge({'学':empty},'学','production',false,'校')['学'];assert(recorded.production.attempts===1&&recorded.distractors['校']===1,'Knowledge recording failed');assert(recorded.educationEvidence?.lastMode==='production'&&recorded.educationEvidence?.lastCorrect===false,'Educational evidence was not persisted');assert(typeof core.educationSchedulerSignal==='function','Scheduler evidence adapter is missing');const signal=core.educationSchedulerSignal(recorded);assert(signal.version===1&&signal.mastery>=0&&signal.mastery<=1&&signal.weakestSkillMastery>=0&&signal.weakestSkillMastery<=1,'Scheduler evidence signal is not bounded');
const distractorTarget={character:'学',on:['ガク'],kun:['まなぶ'],meaning:['study','learning'],strokes:8,grade:1,frequency:100};const distractorCandidates=[{character:'校',on:['コウ'],meaning:['school'],strokes:10,grade:1,frequency:110},{character:'楽',on:['ガク'],meaning:['comfort'],strokes:13,grade:1,frequency:120},{character:'習',on:['シュウ'],meaning:['learn'],strokes:11,grade:1,frequency:90},{character:'語',on:['ゴ'],meaning:['language'],strokes:14,grade:2,frequency:800}];assert(typeof core.scoreDistractor==='function'&&typeof core.chooseDistractors==='function','Smart distractor API missing');assert(core.scoreDistractor(distractorTarget,distractorCandidates[1],{})>core.scoreDistractor(distractorTarget,distractorCandidates[0],{}),'Shared reading distractor did not receive higher score');const picked=core.chooseDistractors(distractorTarget,distractorCandidates,{習:2},2);assert(picked.length===2&&picked.some(x=>x.character==='習'),'Prior wrong distractor history did not influence ranking');assert(!picked.some(x=>x.character==='学'),'Target Kanji leaked into distractor list');const ambiguous={character:'仮',on:['ガク'],meaning:['study'],strokes:8,grade:1,frequency:100};assert(core.distractorAmbiguous(distractorTarget,ambiguous,[])===true,'Ambiguous distractor was not rejected');const distractorIndex=core.buildDistractorIndex([distractorTarget,...distractorCandidates]);assert(Array.isArray(distractorIndex.reading['ガク']),'Reading candidate index was not built');const indexedPool=core.candidatePool(distractorTarget,[...distractorCandidates],distractorIndex);assert(Array.isArray(indexedPool)&&indexedPool.length>0,'Indexed distractor candidate pool is empty');
assert(sync.includes("from './v1.5-sync-core.js'")&&sync.includes('expectedUpdatedAt')&&sync.includes('MAX_SYNC_ATTEMPTS'),'Sync transport does not use the hardened merge/CAS layer');assert(transportCore.includes("from './v1.5-education-sync-core.js'")&&transportCore.includes("from './v1.5-fsrs-sync-core.js'")&&transportCore.includes('mergeKnowledge')&&transportCore.includes('mergeReviewEvents'),'Sync merge does not cover all educational and review state');assert(syncCore.includes('production')&&syncCore.includes('vocabulary')&&syncCore.includes('context')&&syncCore.includes('distractors')&&syncCore.includes('stage')&&syncCore.includes('educationEvidence'),'Educational sync core does not cover all modes/metadata');assert(migration.includes('e.schemaVersion=2')&&migration.includes("const KEY='kanji5-v1.2-knowledge',META='kanji5-v1.4-education-meta',VERSION=2"),'Migration version/schema contract missing');assert(migration.includes('MIGRATIONS=Object.freeze({1:migrateV1,2:migrateV2})'),'Real migration chain is missing');
assert(ui.includes('CORE.selectEducationItem')&&ui.includes('CORE.chooseBestExercise')&&ui.includes('CORE.gradeMeaning')&&ui.includes('CORE.gradeReading')&&ui.includes('CORE.recordKnowledge'),'UI must use the canonical education core');assert(ui.includes('fetchContextSentences')||network.includes('export async function fetchContextSentences'),'Context sentence API wiring missing');assert(ui.includes('edu.sentence.text.replaceAll(item.character'),'Context exercise does not blank the target Kanji');assert(ui.includes('edu.sentence.english'),'Context exercise is missing English translation');assert(!ui.includes('v14EduProductionInput'),'Typed production UI must be replaced by MCQ in v1.5');assert(ui.includes("prompt='برای معنی زیر، کانجی مناسب را انتخاب کن.'")&&ui.includes('renderChoices(chooseChoices(item))'),'Production MCQ flow missing');assert(ui.includes('context')&&ui.includes('vocabulary'),'Vocabulary/context missing');assert(ui.includes('تمرین بعدی'),'Next exercise flow missing');assert(ui.includes('CORE.chooseDistractors'),'UI does not use canonical smart distractors');const selectIndex=ui.indexOf('const item=CORE.selectEducationItem');const modeIndex=ui.indexOf('let mode=');const wordFetchIndex=ui.indexOf('fetchWords(item.character)');const contextFetchIndex=ui.indexOf('fetchContextSentences(item.character)');assert(selectIndex>=0&&modeIndex>selectIndex,'Adaptive item/mode selection ordering is invalid');assert(ui.includes('adaptiveApi?.nextMode?.(modes)')&&ui.includes('CORE.chooseBestExercise(item,stats,modes'),'Adaptive mode selection/fallback wiring missing');assert(wordFetchIndex>modeIndex,'Vocabulary API is fetched before exercise mode is selected');assert(contextFetchIndex>modeIndex,'Context data is fetched before exercise mode selection');assert(!ui.includes("const words=await fetchWords(item.character);const finalModes"),'Unconditional vocabulary fetch regression detected');assert(ui.includes("modes.filter(x=>x==='meaning'||x==='reading'||x==='production')"),'Unsafe API fallback is missing');assert(ui.includes("const check=(edu.mode==='meaning'||edu.mode==='reading')?"),'MCQ action-state fix is missing');assert(!ui.includes('function escapeHTML'),'Legacy local escaping helper must not remain after v1.5 boundary extraction');assert(ui.includes('CORE.chooseDistractors'),'Canonical distractor selection must remain UI-wired through the core');assert(sw.includes('TATOEBA_ORIGIN')&&sw.includes("u.pathname.startsWith('/v1/sentences')"),'Tatoeba service-worker cache missing');assert(network.includes('https://api.tatoeba.org'),'Network adapter must own the Tatoeba endpoint');assert(!index.includes('id="v1.3-education"')&&!index.includes('id="v1.3-p1"'),'Legacy v1.3 education styles remain');
console.log('Kanji 5 v1.6/v1.4 education regression tests passed.');