(()=>{
'use strict';
if(window.__KANJI5_V16_SESSION_ANALYTICS__)return;
const state=window.__KANJI5_STATE__;
if(!state)throw new Error('KANJI5_STATE_REQUIRED');
window.__KANJI5_V16_SESSION_ANALYTICS__=true;
const MODES=['meaning','reading','production','vocabulary','context'];
const LABELS={meaning:'معنی',reading:'خوانش',production:'تولید',vocabulary:'واژگان',context:'بافت'};
const fa=v=>Number(v||0).toLocaleString('fa-IR');
const pct=v=>`${Math.round(Math.max(0,Math.min(100,Number(v)||0)))}٪`;
const signedPct=v=>{const n=Math.round(Number(v)||0);return`${n>=0?'+':''}${n}٪`};
function normalize(s){const a=Math.max(0,Number(s?.attempts)||0),c=Math.min(a,Math.max(0,Number(s?.correct)||0));return{attempts:a,correct:c,accuracy:a?c/a*100:0}}
function sessions(){return(state.readSessionHistory?.()||[]).filter(x=>x&&x.status!=='active'&&x.endedAt).slice(-7)}
function aggregate(rows){const out={};for(const m of MODES)out[m]=normalize();for(const row of rows){for(const m of MODES){const s=normalize(row.modeResults?.[m]);out[m].attempts+=s.attempts;out[m].correct+=s.correct}}for(const m of MODES)out[m].accuracy=out[m].attempts?out[m].correct/out[m].attempts*100:0;return out}
function render(){const host=document.getElementById('v16Session');if(!host)return;let box=document.getElementById('v16SessionAnalytics');if(!box){box=document.createElement('section');box.id='v16SessionAnalytics';box.style.cssText='margin-top:17px;padding:13px;border:1px solid var(--line,#e5e7eb);border-radius:14px;background:#fff';(document.getElementById('v16History')?.parentElement||host).appendChild(box)}const rows=sessions();if(!rows.length){box.innerHTML='<h3 style="font-size:13px;margin:0 0 7px">روند عملکرد</h3><div style="font-size:12px;color:var(--muted,#6b7280)">پس از پایان اولین جلسه، روند عملکرد اینجا نمایش داده می‌شود.</div>';return}const totalReviews=rows.reduce((n,x)=>n+(Number(x.reviews)||0),0);const latest=aggregate(rows.slice(-3));const previous=aggregate(rows.slice(-6,-3));const trend=latest.meaning.attempts+latest.reading.attempts+latest.production.attempts+latest.vocabulary.attempts+latest.context.attempts;const prior=previous.meaning.attempts+previous.reading.attempts+previous.production.attempts+previous.vocabulary.attempts+previous.context.attempts;const latestRate=trend?Object.values(latest).reduce((n,s)=>n+s.correct,0)/trend*100:0;const priorRate=prior?Object.values(previous).reduce((n,s)=>n+s.correct,0)/prior*100:latestRate;const delta=latestRate-priorRate;const best=MODES.map(m=>({m,s:latest[m]})).filter(x=>x.s.attempts).sort((a,b)=>b.s.accuracy-a.s.accuracy)[0];const weak=MODES.map(m=>({m,s:latest[m]})).filter(x=>x.s.attempts).sort((a,b)=>a.s.accuracy-b.s.accuracy)[0];box.innerHTML=`<h3 style="font-size:13px;margin:0 0 8px">روند عملکرد (۷ جلسهٔ اخیر)</h3><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:9px"><div><strong style="font-size:17px">${fa(rows.length)}</strong><div style="font-size:11px;color:var(--muted,#6b7280)">جلسه</div></div><div><strong style="font-size:17px">${fa(totalReviews)}</strong><div style="font-size:11px;color:var(--muted,#6b7280)">مرور FSRS</div></div><div><strong style="font-size:17px">${signedPct(delta)}</strong><div style="font-size:11px;color:var(--muted,#6b7280)">تغییر دقت</div></div></div><div style="display:grid;grid-template-columns:repeat(2,1fr);gap:6px">${MODES.map(m=>{const s=latest[m];return`<span>${LABELS[m]}: ${s.attempts?`${fa(s.correct)}/${fa(s.attempts)} · ${pct(s.accuracy)}`:'—'}</span>`}).join('')}</div><div style="margin-top:8px;font-size:11px;color:#4b5563">قوی‌ترین: ${best?LABELS[best.m]:'—'} · نیازمند توجه: ${weak?LABELS[weak.m]:'—'}</div>`}
render();setInterval(render,500);
})();