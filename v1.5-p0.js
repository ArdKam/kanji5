(()=>{
'use strict';
if(window.__KANJI5_V15_P0__)return;
const corePromise=import('./v1.5-recall-core.js');
const state=window.__KANJI5_STATE__;
if(!state)throw new Error('KANJI5_STATE_REQUIRED');
window.__KANJI5_V15_P0__=true;
let coreApi=null;
const $=(selector,root)=>root?.querySelector?.(selector)||null;
async function ensureCore(){return coreApi||(coreApi=await corePromise)}
function getDeck(){const prefetched=window.__KANJI5_P0_DATA;if(Array.isArray(prefetched)&&prefetched.length)return prefetched;return state.readDeck()}
function ensureComponentEntry(character){const all=state.readComponents();if(!all[character]||typeof all[character]!=='object')all[character]={meaning:{},reading:{},updatedAt:null};if(!all[character].meaning||typeof all[character].meaning!=='object')all[character].meaning={};if(!all[character].reading||typeof all[character].reading!=='object')all[character].reading={};return all}
async function writeSchedulerComponentEvidence(character){const all=state.readComponents(),entry=all[character],knowledge=state.readKnowledge();if(!entry)return;const target=knowledge[character]&&typeof knowledge[character]==='object'?knowledge[character]:{};const {componentSignal}=await ensureCore();target.componentEvidence={...componentSignal(entry),updatedAt:entry.updatedAt||new Date().toISOString(),version:1};knowledge[character]=target;state.writeKnowledge(knowledge)}
async function getFocusComponent(character,mode){const item=getDeck().find(value=>value&&value.character===character);if(!item)return null;const all=ensureComponentEntry(character);const {selectFocus}=await ensureCore();return selectFocus(item,mode,all[character][mode]||{})}
async function recordFocusedRecall(character,mode,focus,outcome){if(!character||!focus)return;const all=ensureComponentEntry(character),{applyRecallOutcome}=await ensureCore();const nextEntry=applyRecallOutcome(all[character],mode,focus,outcome,new Date().toISOString());all[character]=nextEntry;state.writeComponents(all);await writeSchedulerComponentEvidence(character)}
function inferMode(gate){return String(gate?.textContent||'').includes('خوانش')?'reading':'meaning'}
function findRecallPrompt(gate){const nodes=[...gate.children].flatMap(child=>[...child.children]);return nodes.find(node=>{const text=String(node.textContent||'').trim();return text.includes('معنی این کانجی')||text.includes('حداقل یک خوانش')})||null}
async function setRecallPrompt(gate,mode){const nextPrompt=mode==='meaning'?'معنی این کانجی را از حافظه به یاد بیاور و پاسخ خودت را وارد کن.':'حداقل یک خوانش این کانجی را از حافظه به یاد بیاور و پاسخ خودت را وارد کن.';const prompt=findRecallPrompt(gate);if(prompt&&prompt.textContent!==nextPrompt)prompt.textContent=nextPrompt}
async function enhanceRecall(){const gate=$('.v12-recall-gate'),kanji=$('.kanji');if(!gate||!kanji)return;const character=String(kanji.textContent||'').trim();addDontKnowRecall(gate);const mode=String(gate.textContent||'').includes('خوانش')?'reading':'meaning',focus=await getFocusComponent(character,mode);if(!focus)return;gate.dataset.v15Ready=character;gate.dataset.v15Mode=mode;gate.dataset.v15Focus=focus.raw;await setRecallPrompt(gate,mode)}
async function gradeFocusedRecall(mode,answer,focus){const character=String($('.kanji')?.textContent||'').trim(),item=getDeck().find(value=>value&&value.character===character),target=String(focus?.raw||''),canonicalCore=window.__KANJI5_EDU_CORE__;if(!item||!target)return false;await ensureCore();if(mode==='meaning')return Boolean(canonicalCore?.gradeMeaning?.(answer,[target])?.correct);return Boolean(canonicalCore?.gradeReading?.(answer,[target])?.correct)}
function requestCanonicalReveal(){if(!$('#revealBtn'))return false;state.revealed=true;return true}
async function recordRecall(event){const gate=$('.v12-recall-gate');if(!gate)return false;const input=$('#v12RecallInput',gate),character=String($('.kanji')?.textContent||'').trim(),mode=gate.dataset.v15Mode||inferMode(gate),focus=gate.dataset.v15Focus||'';if(!focus){const inferred=await getFocusComponent(character,mode);if(inferred)gate.dataset.v15Focus=inferred.raw}const readyFocus=gate.dataset.v15Focus||'';if(!input||!character||!readyFocus)return false;event?.preventDefault?.();event?.stopPropagation?.();const answer=String(input.value||''),correct=await gradeFocusedRecall(mode,answer,{raw:readyFocus});const result=$('.v12-recall-result',gate);result.className=correct?'v12-recall-result good':'v12-recall-result bad';result.textContent=correct?'✅ پاسخ درست بود':'❌ پاسخ درست نبود';void requestCanonicalReveal();void recordFocusedRecall(character,mode,readyFocus,correct?'correct':'wrong');const knowledge=state.readKnowledge(),entry=knowledge[character]||{},stats=entry[mode]||{attempts:0,correct:0,lastAt:null};stats.attempts=Number(stats.attempts)||0;stats.correct=Number(stats.correct)||0;stats.attempts+=1;if(correct)stats.correct+=1;stats.lastAt=new Date().toISOString();entry[mode]=stats;entry.lastPrompt=mode;knowledge[character]=entry;state.writeKnowledge(knowledge);return correct}
function addDontKnowRecall(){const gate=arguments[0]||$('.v12-recall-gate');return addDontKnowRecallForGate(gate)}
function addDontKnowRecallForGate(gate){if(!gate||$('#v15DontKnowRecall',gate))return;const input=gate.querySelector('input,textarea');if(!input)return;const button=document.createElement('button');button.type='button';button.id='v15DontKnowRecall';button.className='secondary';button.textContent='نمی‌دانم';button.style.width='100%';button.style.marginTop='9px';input.parentElement?.appendChild(button);button.addEventListener('click',async event=>{event.preventDefault();event.stopPropagation();const character=String($('.kanji')?.textContent||'').trim(),mode=gate.dataset.v15Mode||inferMode(gate),focus=gate.dataset.v15Focus||((await getFocusComponent(character,mode))?.raw||'');if(character)state.writeLastAttempt({character,mode,attemptedAt:new Date().toISOString(),hadAttempt:true,correct:false,unknown:true});if(character&&focus)void recordFocusedRecall(character,mode,focus,'unknown');gate.remove();void requestCanonicalReveal()},false)}
function installRecallKeyboard(){if(window.__KANJI5_V15_RECALL_KEYBOARD__)return;window.__KANJI5_V15_RECALL_KEYBOARD__=true;window.addEventListener('keydown',event=>{const input=event.target?.closest?.('#v12RecallInput');if(!input||event.key!=='Enter')return;const gate=input.closest('.v12-recall-gate');if(!gate)return;const submit=$('#v12SubmitRecall',gate);if(!submit)return;event.preventDefault();event.stopImmediatePropagation();submit.click()},true)}
function startTargetedObservers(){const studyRoot=document.getElementById("studyPanel")||document.getElementById("study");if(!studyRoot)return;const observer=new MutationObserver(()=>{void enhanceRecall();syncEducationBusyState()});observer.observe(studyRoot,{childList:true,subtree:true});void enhanceRecall();syncEducationBusyState()}
function installRecallWindowCapture(){window.addEventListener('click',event=>{if(event.target?.id!=='revealBtn')return;queueMicrotask(()=>{const gate=$('.v12-recall-gate');if(gate)void enhanceRecall()});setTimeout(()=>{const gate=$('.v12-recall-gate');if(gate)void addDontKnowRecall(gate)},0)},true)}
function installAccessibilityEnhancements(){if($('#v15-a11y-style'))return;const style=document.createElement('style');style.id='v15-a11y-style';style.textContent='button:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible,[role="button"]:focus-visible{outline:3px solid var(--accent,var(--primary,#4f46e5));outline-offset:3px}@media(prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:.01ms!important;animation-iteration-count:1!important}}';document.head.appendChild(style)}
function syncEducationBusyState(){const root=document.getElementById('educationPanel')||document.querySelector('.education-panel');if(!root)return;const busy=root.getAttribute('aria-busy')==='true';if(root.getAttribute('aria-busy')!==(busy?'true':'false'))root.setAttribute('aria-busy',busy?'true':'false');const empty=root.querySelector('.empty');if(empty&&empty.getAttribute('role')!=='status')empty.setAttribute('role','status')}
function guardBusyEducationClicks(event){const control=event.target?.closest?.('button,[role="button"]');if(!control||control.id==='v15DontKnowRecall')return;const root=control.closest?.('#educationPanel,.education-panel');if(!root||root.getAttribute('aria-busy')!=='true')return;event.preventDefault();event.stopImmediatePropagation()}
window.__KANJI5_V15_RECALL_API__=Object.freeze({refresh:()=>enhanceRecall(),ensureDontKnow:addDontKnowRecall});
if(typeof document!=='undefined'){
 installRecallWindowCapture();
 installRecallKeyboard();
 installAccessibilityEnhancements();
 document.addEventListener('click',guardBusyEducationClicks,true);
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startTargetedObservers,{once:true});else startTargetedObservers();
}
})();
