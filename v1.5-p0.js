(()=>{
'use strict';
if(window.__KANJI5_V15_P0__)return;
window.__KANJI5_V15_P0__=true;
const COMPONENT_KEY='kanji5-v1.5-components',KNOWLEDGE_KEY='kanji5-v1.2-knowledge';
const $=(selector,root=document)=>root.querySelector(selector);
function readJSON(key,fallback){try{const raw=localStorage.getItem(key);if(!raw)return fallback;const value=JSON.parse(raw);return value&&typeof value==='object'?value:fallback}catch(_){return fallback}}
function writeJSON(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch(_) {}}
function normalize(value){return String(value==null?'':value).trim().toLowerCase().normalize('NFKC').replace(/[\s\u3000]+/g,'')}
function getDeck(){const data=window.__KANJI5_P0_DATA;if(Array.isArray(data)&&data.length)return data;const cached=readJSON('kanji5-deck',[]);return Array.isArray(cached)?cached:[]}
function ensureComponentEntry(character){const all=readJSON(COMPONENT_KEY,{});if(!all[character]||typeof all[character]!=='object')all[character]={meaning:{},reading:{},updatedAt:null};if(!all[character].meaning||typeof all[character].meaning!=='object')all[character].meaning={};if(!all[character].reading||typeof all[character].reading!=='object')all[character].reading={};return all}
function componentAccuracy(stats){const attempts=Math.max(0,Number(stats?.attempts)||0),correct=Math.min(attempts,Math.max(0,Number(stats?.correct)||0));if(!attempts)return 0;const score=Number.isFinite(Number(stats?.score))?Math.max(0,Math.min(attempts,Number(stats.score))):correct;return(score+1)/(attempts+2)}
function componentSignal(entry){const out={};for(const mode of ['meaning','reading']){const values=Object.values(entry?.[mode]||{}).filter(v=>v&&typeof v==='object'&&(Number(v.attempts)||0)>0);if(!values.length){out[mode]={accuracy:null,weakness:null,attempts:0,lastAt:null};continue}let weight=0,total=0,lastAt=null;for(const value of values){const attempts=Math.max(0,Number(value.attempts)||0);weight+=attempts;total+=componentAccuracy(value)*attempts;const at=typeof value.lastAt==='string'?value.lastAt:'';if(at&&(lastAt===null||at>lastAt))lastAt=at}const accuracy=weight?total/weight:0;out[mode]={accuracy,weakness:Math.max(0,Math.min(1,1-accuracy)),attempts:weight,lastAt}}return out}
function writeSchedulerComponentEvidence(character){try{const all=readJSON(COMPONENT_KEY,{}),entry=all[character],signal=componentSignal(entry);const knowledge=readJSON(KNOWLEDGE_KEY,{});const target=knowledge[character]&&typeof knowledge[character]==='object'?knowledge[character]:{};target.componentEvidence={...signal,updatedAt:entry?.updatedAt||new Date().toISOString(),version:1};knowledge[character]=target;writeJSON(KNOWLEDGE_KEY,knowledge)}catch(_) {}}
function getFocusComponent(character,mode){const item=getDeck().find(entry=>entry&&entry.character===character);if(!item)return null;const state=ensureComponentEntry(character)[character][mode],values=mode==='reading'?[...(item.on||[]),...(item.kun||[])]:[...(item.meaning||[])],candidates=values.map(raw=>({raw,key:normalize(raw),accuracy:componentAccuracy(state[normalize(raw)]||{})}));candidates.sort((a,b)=>a.accuracy-b.accuracy);return candidates[0]||null}
function gradeFocusedRecall(mode,answer,focus){const core=window.__KANJI5_EDU_CORE__;if(!core||!focus)return false;return mode==='reading'?Boolean(core.gradeReading(answer,[focus.raw]).correct):Boolean(core.gradeMeaning(answer,[focus.raw]).correct)}
function recordFocusedRecall(character,mode,focus,outcome){const all=ensureComponentEntry(character),bucket=all[character][mode],key=normalize(focus),stats=bucket[key]||{attempts:0,correct:0,unknown:0,score:0,lastAt:null};stats.attempts+=1;if(outcome==='correct'){stats.correct+=1;stats.score+=1}else if(outcome==='unknown'){stats.unknown=(Number(stats.unknown)||0)+1;stats.score+=.25}stats.lastAt=new Date().toISOString();bucket[key]=stats;all[character].updatedAt=stats.lastAt;writeJSON(COMPONENT_KEY,all);writeSchedulerComponentEvidence(character)}
function installTatoebaFetchDeduper(){if(window.__KANJI5_V15_TATOEBA_DEDUP__)return;const nativeFetch=window.fetch?.bind(window);if(!nativeFetch)return;const inflight=new Map;window.fetch=(input,init)=>{const url=typeof input==='string'?input:input?.url||'',method=String(init?.method||input?.method||'GET').toUpperCase();if(method!=='GET'||!url.startsWith('https://api.tatoeba.org/v1/sentences'))return nativeFetch(input,init);const pending=inflight.get(url);if(pending)return pending.then(response=>response.clone());const request=nativeFetch(input,init).finally(()=>inflight.delete(url));inflight.set(url,request);return request.then(response=>response.clone())};window.__KANJI5_V15_TATOEBA_DEDUP__=true}
function installAccessibilityEnhancements(){if($('#v15-a11y-style'))return;const style=document.createElement('style');style.id='v15-a11y-style';style.textContent='button:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible,[role="button"]:focus-visible{outline:3px solid var(--accent,var(--primary,#4f46e5));outline-offset:3px} @media(prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:.01ms!important;animation-iteration-count:1!important;scroll-behavior:auto!important;transition-duration:.01ms!important}} .v15-pending[aria-busy="true"]{cursor:wait}';document.head.appendChild(style)}
function syncEducationBusyState(){const root=$('#educationPanel')||$('.education-panel')||$('#studyPanel');if(!root)return;const empty=root.querySelector('.v14-edu-empty'),text=String(empty?.textContent||'');const busy=text.includes('در حال آماده‌سازی');root.setAttribute('aria-busy',busy?'true':'false');root.classList.toggle('v15-pending',busy);if(empty&&busy)empty.setAttribute('role','status')}
function guardBusyEducationClicks(event){const control=event.target?.closest?.('button,[role="button"]');if(!control||control.id==='v15DontKnowRecall')return;const root=control.closest?.('#educationPanel,.education-panel');if(!root||root.getAttribute('aria-busy')!=='true')return;event.preventDefault();event.stopImmediatePropagation()}
function enhanceRecall(){const gate=$('.v12-recall-gate'),kanji=$('.kanji');if(!gate||!kanji)return;const character=String(kanji.textContent||'').trim();if(!character||gate.dataset.v15Ready===character)return;const mode=String(gate.textContent||'').includes('خوانش')?'reading':'meaning',focus=getFocusComponent(character,mode);if(!focus)return;gate.dataset.v15Ready=character;gate.dataset.v15Mode=mode;gate.dataset.v15Focus=focus.raw;const prompt=[...gate.querySelectorAll('div')].find(node=>{const text=String(node.textContent||'').trim();return text.includes('معنی این کانجی')||text.includes('حداقل یک خوانش')});if(prompt)prompt.textContent=mode==='meaning'?'معنی این کانجی را از حافظه به یاد بیاور و پاسخ خودت را وارد کن.':'حداقل یک خوانش این کانجی را از حافظه به یاد بیاور و پاسخ خودت را وارد کن.'}
function recordRecall(event){const gate=event.target?.closest?.('.v12-recall-gate'),button=event.target?.closest?.('button');if(!gate||!button||button.id==='v15DontKnowRecall')return;const kanji=$('.kanji'),input=gate.querySelector('input,textarea');if(!kanji||!input)return;const character=String(kanji.textContent||'').trim(),answer=String(input.value||'').trim(),mode=gate.dataset.v15Mode||'meaning',focus=gate.dataset.v15Focus||'';if(!character||!answer||!focus)return;const correct=gradeFocusedRecall(mode,answer,{raw:focus});recordFocusedRecall(character,mode,focus,correct?'correct':'wrong');const result=gate.querySelector('.v12-recall-result');if(result){result.className=correct?'v12-recall-result good':'v12-recall-result bad';result.textContent=correct?'✅ پاسخ درست بود':'❌ پاسخ درست نبود'}revealAfterUnknown(gate)}
function revealAfterUnknown(gate){const answer=gate.closest?.('#study')?.querySelector?.('.answer')||$('.answer'),answerBox=$('#answerBox'),ratings=$('#ratings');if(answer)answer.classList.add('show');if(answerBox)answerBox.classList.add('show');if(ratings){ratings.style.display='grid';ratings.classList.add('show');ratings.querySelector('button')?.focus()}gate.remove()}
function addDontKnowRecall(){const gate=$('.v12-recall-gate');if(!gate||$('#v15DontKnowRecall',gate))return;const input=gate.querySelector('input,textarea');if(!input)return;const button=document.createElement('button');button.type='button';button.id='v15DontKnowRecall';button.className='secondary';button.textContent='نمی‌دانم';button.addEventListener('click',()=>{const kanji=$('.kanji'),character=String(kanji?.textContent||'').trim(),mode=gate.dataset.v15Mode||'meaning',focus=gate.dataset.v15Focus||getFocusComponent(character,mode)?.raw||'';if(character){try{localStorage.setItem('kanji5-v1.2-last-attempt',JSON.stringify({character,mode,attemptedAt:new Date().toISOString(),hadAttempt:true,correct:false,unknown:true}))}catch(_) {}}if(character&&focus)recordFocusedRecall(character,mode,focus,'unknown');const result=gate.querySelector('.v12-recall-result');if(result){result.className='v12-recall-result unknown';result.textContent='این مورد را نمی‌دانستم'}revealAfterUnknown(gate)});input.parentElement?.appendChild(button)}
function scheduleRecallControls(){queueMicrotask(()=>{enhanceRecall();addDontKnowRecall()})}
function startTargetedObservers(){installTatoebaFetchDeduper();installAccessibilityEnhancements();const studyRoot=$('#educationPanel')||$('.education-panel')||$('#studyPanel')||$('#study');if(studyRoot){const observer=new MutationObserver(()=>{enhanceRecall();addDontKnowRecall();syncEducationBusyState()});observer.observe(studyRoot,{childList:true,subtree:true})}syncEducationBusyState();enhanceRecall();addDontKnowRecall()}
document.addEventListener('click',guardBusyEducationClicks,true);
document.addEventListener('click',event=>{const target=event.target;if(target?.id==='revealBtn')scheduleRecallControls()},true);
document.addEventListener('click',recordRecall,true);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startTargetedObservers,{once:true});else startTargetedObservers();
})();
